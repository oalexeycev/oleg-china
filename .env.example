# Ключ OpenRouter (только на сервере / в Vercel — не VITE_*).
# Только значение sk-or-v1-… одной строкой: без "Bearer ", без кавычек "…" вокруг.
# После добавления на Vercel сделай Redeploy; для Preview-веток включи переменную отдельно.
OPENROUTER_API_KEY=
